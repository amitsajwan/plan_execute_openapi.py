# Core LangGraph and LangChain components (adjust versions as needed)
langchain>=0.1.0,<0.2.0
langgraph>=0.0.30,<0.1.0
# Pydantic for data models (often installed with langchain/langgraph)
pydantic>=2.0.0,<3.0.0

# For persistent caching
diskcache>=5.0.0,<6.0.0

# For YAML parsing
pyyaml>=6.0,<7.0

# For environment variables
python-dotenv>=1.0.0,<2.0.0

# --- LLM Client Libraries ---
# Add your specific LLM client libraries here, e.g.:
langchain-openai>=0.1.0,<0.2.0
# langchain-google-genai>=0.0.5,<0.1.0
# langchain-community # Might be needed for some integrations

# Add other dependencies for the FastAPI app
fastapi>=0.100.0,<1.0.0
uvicorn>=0.20.0,<1.0.0
websockets>=11.0,<13.0 # For WebSocket support in FastAPI/Uvicorn
jinja2>=3.0.0,<4.0.0 # Only if you use the HTML template

# --- Removed from previous iterations ---
# requests>=2.25.0,<3.0.0 # Not needed as no HTTP requests are made
# jsonpath-ng>=1.5.0,<2.0.0 # Not needed as no execution/data extraction occurs
