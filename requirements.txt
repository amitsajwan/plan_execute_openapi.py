fastapi
uvicorn[standard] # Includes websockets
langchain
langgraph
# Add your specific LLM provider library, e.g.:
# langchain-openai
# langchain-anthropic
# langchain-google-genai 
python-dotenv # For managing environment variables (API keys)
PyYAML # For YAML parsing of OpenAPI specs
diskcache # For persistent caching of parsed schemas
pydantic >= 2.0 # Ensure Pydantic v2 for model_validator etc.
